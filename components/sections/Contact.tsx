import { SectionTitle } from "../text";
import { ScrollSpyContainer } from "../ui/containers";

export const Contact = () => {
  return (
    <ScrollSpyContainer section="contact">
      <SectionTitle>Contact</SectionTitle>
    </ScrollSpyContainer>
  );
};
