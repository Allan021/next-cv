import { SectionTitle } from "../ui/text";
import { ScrollSpyContainer } from "../ui/containers";

export const Portafolio = () => {
  return (
    <ScrollSpyContainer section="portafolio">
      <SectionTitle>Portafolio</SectionTitle>
    </ScrollSpyContainer>
  );
};
