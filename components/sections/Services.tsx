import { SectionTitle } from "../ui/text";
import { ScrollSpyContainer } from "../ui/containers";

export const ServicesSection = () => {
  return (
    <ScrollSpyContainer section="services">
      <SectionTitle>Services</SectionTitle>
    </ScrollSpyContainer>
  );
};
